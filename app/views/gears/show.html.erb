<!-- app/views/gears/show.html.erb -->
<div class="max-w-4xl mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6 text-gray-800"><%= @gear.title %></h1>

  <div class="prose lg:prose-xl text-gray-700">
    <%= @gear.content %>
  </div>

  <div class="mt-6">
    <%= link_to "← Back to Gear List",
                gears_path,
                class: "text-blue-600 hover:underline" %>
  </div>
</div>
