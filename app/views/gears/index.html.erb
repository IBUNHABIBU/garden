<!-- app/views/gears/index.html.erb -->
<div class="max-w-4xl mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6 text-gray-800">Kilimanjaro Gear Guide</h1>

  <% @gears.each do |gear| %>
    <div class="bg-white shadow-md rounded-2xl p-6 mb-6 hover:shadow-lg transition">
      <!-- Preview (truncate the ActionText content) -->
      <div class="prose prose-sm text-gray-600 mb-4">
        <%= truncate(strip_tags(gear.content.to_s), length: 250) %>
      </div>

      <%= link_to "Read Full Gear List →",
                  gear_path(gear),
                  class: "inline-block bg-blue-600 text-white px-4 py-2 rounded-xl hover:bg-blue-700 transition" %>
    </div>
  <% end %>
</div>
