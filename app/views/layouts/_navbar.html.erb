<nav class="bg-blue-500 shadow-md" 
    data-controller="navbar"
    data-navbar-expanded-value="false"
>
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <%= link_to 'Company', root_path, class: 'text-white font-bold text-xl hover:opacity-90 transition-opacity' %>
        <button class="md:hidden text-white p-2 focus:outline-none"
         data-action="navbar#toggle"
         aria-label="Toggle menu"
        >
            <div class="w-6 flex flex-col gap-1.5">
                <span class="h-0.5 bg-white transition-all duration-200 origin-left" data-navbar-target="bar1"></span>
                <span class="h-0.5 bg-white transition-all duration-200" data-navbar-target="bar2"></span>
                <span class="h-0.5 bg-white transition-all duration-200 origin-left" data-navbar-target="bar3"></span>
            </div>
        </button>
    </div>

    <ul class="hidden md:flex items-center gap-6">
        <% nav_links.each do |link| %>
            <li>
                 <%= link_to link[:name], link[:path], 
              class: "text-white font-medium hover:opacity-80 transition-opacity #{current_page?(link[:path]) ? 'relative after:absolute after:bottom-0 after:left-0 after:w-full after:h-0.5 after:bg-white' : ''}" %>
            </li>
        <% end %>
    </ul>
</nav>