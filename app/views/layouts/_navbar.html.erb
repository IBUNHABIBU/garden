<nav class="bg-primary text-white shadow-lg">
  <div class="container mx-auto px-6 py-4">
    <div class="flex justify-between items-center">
      <div class="flex items-center space-x-4">
        <!-- Logo -->
        <div class="flex items-center">
          <span class="text-2xl font-bold text-accent">MARVELLOUS</span>
          <span class="text-2xl font-bold text-pink-500">SAFARIS</span>
        </div>
      </div>
      
      <!-- Desktop Menu -->
      <div class="hidden md:flex items-center space-x-8">
        <%= link_to "Home", root_path, class: "hover:text-accent transition" %>
        <%= link_to "Tours", services_path, class: "hover:text-accent transition" %>
        <%= link_to "About", root_path, class: "hover:text-accent transition" %>
        <%= link_to "Contact", root_path, class: "hover:text-accent transition" %>
        <%= link_to "Book Now", new_booking_path, class: "bg-accent hover:bg-accent-dark text-primary font-bold py-2 px-6 rounded-full transition" %>
      </div>
    </div>
  </div>
</nav>