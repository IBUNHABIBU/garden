<nav class="navbar" data-controller="navbar">
  <div class="nav-container">
    <div class="nav-content">
      <div class="nav-logo-group">
        <!-- Logo -->
        <div class="nav-logo">
          <span class="nav-logo-part1">MARVELLOUS</span>
          <span class="nav-logo-part2">SAFARIS</span>
        </div>
        
        <!-- Mobile Menu Button -->
        <button 
          class="mobile-menu-button" 
          data-action="click->navbar#toggleMenu"
          aria-label="Toggle menu"
        >
          <svg class="menu-icon" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
            <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/>
          </svg>
        </button>
      </div>
      
      <!-- Menu -->
      <div 
        class="nav-menu" 
        data-navbar-target="menu"
        data-transition-enter="transition ease-out duration-200"
        data-transition-enter-from="opacity-0 scale-95"
        data-transition-enter-to="opacity-100 scale-100"
        data-transition-leave="transition ease-in duration-150"
        data-transition-leave-from="opacity-100 scale-100"
        data-transition-leave-to="opacity-0 scale-95"
      >
        <%= link_to "Home", root_path, class: "nav-link", data: { turbo_frame: "_top" } %>
        <%= link_to "Tours", services_path, class: "nav-link", data: { turbo_frame: "_top" } %>
        <%= link_to "About", about_path, class: "nav-link", data: { turbo_frame: "_top" } %>
        <%= link_to "Contact", contact_path, class: "nav-link", data: { turbo_frame: "_top" } %>
        <%= link_to "Book Now", new_booking_path, class: "nav-button", data: { turbo_frame: "_top" } %>
      </div>
    </div>
  </div>
</nav>